package com.bedwars.listeners;

import com.bedwars.BedwarsPlugin;
import org.bukkit.event.Listener;

/**
 * Shop inventory interactions are handled by GUIManager.
 * This class is kept as a placeholder for any future shop-specific logic
 * that does not fit in GUIManager (e.g. custom item purchase side-effects).
 */
public class ShopListener implements Listener {

    public ShopListener(BedwarsPlugin plugin) {
        // All click routing moved to GUIManager
    }
}
